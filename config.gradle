ext {
    //Android
    android = [
            compileSdkVersion: 29,
            buildToolsVersion: "29.0.2",
            applicationId    : "com.example.middleexpansionrecycler",
            minSdkVersion    : 19,
            targetSdkVersion : 29,
            versionCode      : 1,
            versionName      : "1.0.0"
    ]

    //依赖版本
    depVersion = [
            appcompat: "1.0.2",
            recyclerview: "1.0.0",
            gson     : "2.8.5",
    ]

    //依赖配置
    dependencies = [
            appcompat   : "androidx.appcompat:appcompat:${depVersion.appcompat}",
            recyclerview: "androidx.recyclerview:recyclerview:${depVersion.recyclerview}",
            gson        : "com.google.code.gson:gson:${depVersion.gson}",
    ]
}